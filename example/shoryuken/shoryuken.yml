aws:
  access_key_id: ABCDEFG123456789       # or <%= ENV['AWS_ACCESS_KEY_ID'] %>
  secret_access_key: 9876543210ZYX      # or <%= ENV['AWS_SECRET_ACCESS_KEY'] %>
  region: ap-southeast-2                # or <%= ENV['AWS_REGION'] %>
concurrency: 25  # The number of allocated threads to process messages. Default 25
delay: 5         # The delay in seconds to pause a queue when it's empty. Default 0
queues:
  - [connector_development_default, 2]
